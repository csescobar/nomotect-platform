# Certification Audit Extension

This fixture models a separately packaged trusted extension. It declares a
capability, bounded configuration and documentation files, a namespaced
migration path, routes and assets. Certification loads its entrypoint only
after a successful read-only preflight.
