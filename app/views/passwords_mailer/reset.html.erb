<h1><%= t("passwords.mailer.heading") %></h1>
<p><%= t("passwords.mailer.instructions") %></p>
<p><%= link_to t("passwords.mailer.action"), edit_password_url(@token) %></p>
<p><%= t("passwords.mailer.expiration") %></p>
